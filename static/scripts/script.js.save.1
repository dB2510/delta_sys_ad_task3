function mainf(){
alert(document.getElementById('rem').checked);
if(document.getElementById('rem').checked)
{
console.log("cookie called");
user = document.getElementById("user").value;
console.log('user');
name ="userId";
alert(user);
document.cookie=name+"="+user+";expires=Sat,30 Jun 2018 12:00:00 UTC";
console.log('really');
}
}

function validate(){

user = document.regform.username.value
pass = document.regform.password.value
fullname = document.regform.fullname.value
email = document.myForm.email.value
var format = /[ !@#$%^&*()_+\-=\[\]{};':"\\|,.<>\/?]/;
var format1 = /[abcdefgh str.match(/[a-z]/i
error = {}
if(user.length < 5 && user.length > 10)
{
error.append("Username must be between 5 to 10 characters")
}
if(pass.value <5 || 
